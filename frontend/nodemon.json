{
  "$schema": "https://json.schemastore.org/nodemon.json",
  "exec": "electron . --no-sandbox --enable-features=WebRTCPipeWireCapturer",
  "watch": ["build"],
  "ignore": ["public/build", ".turbo"],
  "delay": 1500
}
